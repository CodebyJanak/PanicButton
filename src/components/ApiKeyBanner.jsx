// API key is now handled server-side — this component is no longer needed
// Keeping file to avoid import errors in case any component references it
export default function ApiKeyBanner() { return null; }
